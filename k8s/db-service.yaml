apiVersion: v1
kind: Service
metadata:
  name: taskmanager-db
spec:
  selector:
    app: taskmanager-db
  ports:
    - port: 5432
      targetPort: 5432
  # No "type" specified = ClusterIP (default) = only reachable from inside
  # the cluster, which is exactly what we want for a database
